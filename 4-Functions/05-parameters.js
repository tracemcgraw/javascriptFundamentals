//The parameters are the variables found in the parenthesis
//can be named anything but has to stay that name through the function

function petNameAndBreed(name, type){
    var petDetails = name + ', '+ type;
    console.log(petDetails);
    return petDetails;
}