var x = 6;
var yay = (x == 0) ? 'hello' : (x<0) ? "hi" : "goodbye";
console.log(yay)

var age = 30;

var myAge = (age >= 25) ? "Yay! You can rent a car!" : (age >= 21) ? "Yay, you can drink!" : (age >=18) ? "Yay, you can vote!" :  "You can't do anything."
//your last one can't have an age restriction function. 
console.log(myAge)

