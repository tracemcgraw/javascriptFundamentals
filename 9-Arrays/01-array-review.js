// Create an array of cars with 3 values, then print all three to the console.
// Create an array with multiple data-types and print each to the console.
// Several of the string methods will also work with arrays.
// Though we'll go further into detail on array methods later, play around with a few of them.

var cars = ['Buggie','truck','van'];
console.log(cars[0]);
console.log(cars[1]);
console.log(cars[2]);

var arr = ['hello', 7, true];
console.log(arr[0]);
console.log(arr[1]);
console.log(arr[2]);